{
  "name": "rollup-plugin-write-output",
  "version": "0.1.1",
  "description": "Inject output chunks to HTML or JSON files.",
  "keywords": [
    "rollup-plugin",
    "inject",
    "output"
  ],
  "main": "index.js",
  "files": [
    "lib"
  ],
  "eslintIgnore": [
    "coverage"
  ],
  "scripts": {
    "test": "eslint **/*.js --cache && c8 --reporter lcov mocha",
    "preversion": "npm test",
    "postversion": "git push --follow-tags && npm publish"
  },
  "repository": "eight04/rollup-plugin-write-output",
  "author": "eight04 <eight04@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "c8": "^7.5.0",
    "eslint": "^7.19.0",
    "mocha": "^8.3.0",
    "rollup": "^2.38.5",
    "string-looks-like": "^0.1.1",
    "tempdir-yaml": "^0.3.0"
  },
  "peerDependencies": {
    "rollup": "^2.38.5"
  }
}
